---
- name: Create container app
  hosts: all
  gather_facts: no
  connection: local

  tasks:

  - name: Create application
    k8s:
      state: present
      definition: "{{ lookup('template', 'all.yml') }}"

