- name: k8s
  hosts: all
  run_once: true
  tasks:

  - name: Get a list of all pods from any namespace
    k8s_info:
      kind: Pod
    register: pod_list
    delegate_to: localhost


  - name: print info
    debug:
      var: pod_list

